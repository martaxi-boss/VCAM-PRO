#!/bin/sh
set -eu

if [ "${1:-}" = "remove" ] || [ "${1:-}" = "purge" ]; then
    if [ -x /var/jb/usr/bin/uicache ]; then
        /var/jb/usr/bin/uicache -u com.vcampro.gate1viewer >/dev/null 2>&1 || true
    fi
fi

exit 0
